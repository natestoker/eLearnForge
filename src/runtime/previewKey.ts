// Shared between the editor (writer) and the standalone player (reader).
// Lives alone so importing it never drags in either app's boot side effects.
export const PREVIEW_KEY = 'elearnforge.preview';
